using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WayPoint : MonoBehaviour
{
    // GENERATE WAYPOINTS / grid based on tags of every gameobject in the world :  an openset, dynamically changed
    // groud => certain number of waypoint


    // CHARACTER class : with velocity and speed


    // GET TARGET WAYPOINTS: For each waypoint, find all available waypoint that is within reach, using character class.
    // USE PHYSICS SCENE: trace out all the points on the curve, if hit obstacles, path is blocked, rule out that waypoints


    // NODE class:
    // Do nothing
    // Hold left
    // Hold right
    // Hold jump
    // Hold left+jump
    // Hold right+jump
}
